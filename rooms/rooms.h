#ifndef ROOMS_H
#define ROOMS_H

#include "../game_state.h"

// Function declarations for each room
void play_room1(GameState *game_state);
void play_room2(GameState *game_state);
void play_room3(GameState *game_state);

#endif // ROOMS_H
